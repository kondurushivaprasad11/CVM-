# CVM-
CVM++ (Custom Virtual Machine in C++) is a custom programming language and runtime environment built entirely from scratch in C++.  It's designed to act as a complete  end-to-end custom scripting language compiler, taking source code scripts and executing them.
